---
description: 全局研发规则（中文、性能、REST、ID、Java Import）
alwaysApply: true
---

# 全局研发规则

## 1) 语言
- 所有回复、注释、提交信息使用中文。
- 代码符号、库名、协议名保持原文，不做翻译。

## 2) 核心原则
- 性能优先：避免 N+1 查询；列表接口必须分页；默认限制返回条数。
- 数据库变更需审批：未经批准不得新增字段；索引/约束/迁移脚本同样需审批。
- 遵循规范化设计，避免冗余存储和重复写入。

## 3) API 规范
- 严格遵循 RESTful：名词语义、复数资源、正确 HTTP 方法与状态码。
- 新接口必须按 RESTful 设计；历史接口逐步治理，不做一次性破坏性重构。

## 4) Snowflake ID 规范
- 前端一律将 ID 按 `string` 处理与传输。
- 禁止将业务 ID 转为 `number` 参与计算、排序或比较。

## 5) Java Import 规范
- 默认使用 `import`。
- 仅当同名类冲突且无法消解时，允许在最小范围使用全限定类名。
- 非冲突场景禁止使用全限定类名。

## 6) 执行要求
- 在满足业务需求前提下，优先选择更高性能方案。
- 涉及数据库结构调整时，先给出影响评估与回滚方案，再实施。
